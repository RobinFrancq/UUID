package logic;

public class UUIDMaster {
	/*
	public static void main(String[] args) {
		if (args[0].equals("createNewUUID")) {
			String sourceEntityId = args[1];
			int entityTypeId = Integer.parseInt(args[2]);
			int sourceId = Integer.parseInt(args[3]);
			
			myUUID.insertNewUUID(sourceEntityId, entityTypeId, sourceId);
		}
		else if (args[0].equals("updateUUID")) {
			String setUUID = args[1];
			String sourceEntityId = args[2];
			int entityTypeId = Integer.parseInt(args[3]);
			int version = Integer.parseInt(args[4]);
			int sourceId = Integer.parseInt(args[5]);
			
			myUUID UUID = new myUUID(setUUID, sourceEntityId, entityTypeId, version, sourceId);
			
			myUUID.updateVersion(UUID);
		}
		else {
			System.out.println("Something went wrong");
		}
	}
	*/
}
